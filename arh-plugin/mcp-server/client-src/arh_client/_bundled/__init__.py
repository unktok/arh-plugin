# Bundled plugin scripts shipped with arh-client so the CLI can install
# Claude Code / Codex hooks without a checkout of the full plugin repo.
# Sources are kept in sync with arh-plugin/scripts/ via tools/sync_bundled.sh.
